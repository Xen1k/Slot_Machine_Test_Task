#pragma once

namespace Settings
{

	// Stop rotation after the barrel rotated on this number of tiles (maximum of the random value)
	const unsigned int maxBarrelTilesToRotate = 5;
	const unsigned int minBarrelTilesToRotate = 2;
	const unsigned int minBarrelRotationSpeed = 200;
	const unsigned int maxBarrelRotationSpeed = 400;
}